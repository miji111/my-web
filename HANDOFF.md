# 할매공방 홈페이지 — 인수인계 문서

**받는 분:** 개선 작업을 맡아주실 개발자 / 디자이너
**넘기는 것:** 정적 HTML 사이트 3페이지 + 이미지 + 원본 기획서
**작성일:** 2026-08-25

---

## 0. 30초 요약

- 빌드 도구 없는 **순수 정적 사이트**입니다. `index.html`을 브라우저로 열면 바로 돌아갑니다.
- 3페이지: **홈 / 메뉴판 / 소식받기**
- 폰트·CSS·JS 외에 **의존성이 없습니다.** npm, 번들러, 프레임워크 전부 없습니다.
- 콘텐츠의 출처는 `SHOP.md` (사장님 인터뷰 정리본)입니다. **글을 고치기 전에 이 문서를 먼저 읽어주세요.**
- 지금 상태는 **디자인 시안에 가깝습니다.** 폼은 화면상에서만 동작하고 서버로 전송되지 않습니다.

---

## 1. 파일 구조

```
my-web/
├── index.html        홈 (1,514줄) ── ⚠ CSS·JS가 파일 안에 인라인으로 들어 있음
├── menu.html         메뉴판 (463줄)
├── subscribe.html    소식받기 (473줄)
│
├── site.css          menu/subscribe 공용 스타일 (183줄)
├── site.js           menu/subscribe 공용 스크립트 (53줄)
├── fold.js           종이 접기 연출 (3페이지 중 index/subscribe에서 사용)
│
├── images/
│   ├── p-*.jpg           상품 사진 6장 (900×900, 흰 배경)
│   ├── anpan-cut.jpg 등  인스타그램 칸에 쓰는 현장 사진 5장
│   └── CREDITS.txt       ⚠ 사진 출처와 라이선스 — 반드시 확인
│
├── SHOP.md           사장님 인터뷰 원본 기획서 (콘텐츠의 유일한 출처)
└── HANDOFF.md        이 문서
```

### 페이지별 링크 구조

세 페이지는 상단 GNB와 하단 4단 푸터로 서로 연결됩니다.

| 위치 | 홈 | 메뉴판 | 소식받기 |
|---|---|---|---|
| GNB | `index.html` | `menu.html` | `subscribe.html` |
| 앵커 | `#story` `#keep` `#mood` `#guest` `#contact` | `#anpan` `#salt` `#walnut` `#more` `#price` `#combo` | `#apply` |

---

## 2. 로컬에서 띄우는 법

파일을 더블클릭해도 열리지만, **`fetch`나 폰트 캐싱 문제를 피하려면 로컬 서버로 여는 걸 권장**합니다.

```bash
# 아무거나 하나
npx serve .
python -m http.server 8000
```

**빌드 과정이 없습니다.** 파일을 고치고 새로고침하면 끝입니다.

---

## 3. 디자인 시스템

### 3-1. 색

전부 CSS 변수입니다. `site.css` 상단과 `index.html`의 `<style>` 상단, **두 곳에 같은 값이 정의되어 있습니다** (§6 참고).

| 변수 | 값 | 쓰이는 곳 |
|---|---|---|
| `--pat-deep` | `#3d1f22` | 제목, 로고 |
| `--pat` | `#6b2f31` | **강조색.** 버튼, 가격, 사장님 말 |
| `--pat-light` | `#8c4a44` | 섹션 라벨, 보조선 |
| `--iron` | `#2a2523` | 어두운 섹션 배경 (무쇠 검정) |
| `--wood` `--wood-light` | `#e8d9c3` `#f5ece0` | 나무 진열대 톤 배경 |
| `--paper` | `#fffdfa` | 본문 배경 |
| `--line` `--line-strong` | `#eae2d6` `#d9cdbb` | 구분선 |
| `--ink` `--ink-mid` `--ink-mute` `--ink-faint` | `#221e1c` → `#b3aaa3` | 글자 4단계 |

색의 근거는 `SHOP.md`의 *"팥 삶은 색(짙은 자주빛 갈색), 무쇠 검정, 나무 진열대의 옅은 나무색"* 입니다.

### 3-2. 글꼴

| 용도 | 폰트 | 비고 |
|---|---|---|
| **상단 로고 워드마크만** | `Black Han Sans` | Google Fonts |
| **그 외 전부** | `Pretendard Variable` | jsDelivr CDN |

> 규칙: **로고 외에는 전부 Pretendard입니다.** 제목도 Pretendard 700/800을 씁니다.

### 3-3. 글자 위계 — 이 7단계만 씁니다

새 요소를 만들 때 임의로 크기를 정하지 말고 아래에서 고르세요.

| 클래스 | 스펙 | 역할 |
|---|---|---|
| `.label` | 10.9px / 700 / 자간 .22em / 팥색 / 대문자 | 섹션 머리 (BEST, MORE…) |
| `h2` | 25.6~32.8px / 700 | 섹션 제목 |
| `h3` | 17.6px / 700 | 카드·항목 제목 |
| `.lead` | 17px / 600 / 진한 먹색 | 섹션당 **한 문장만** — 시선이 떨어지는 지점 |
| 본문 | 15px / 400 | 기본 |
| `.mute` | 13.9px / 400 / `--ink-mute` | 부연·설명·캡션 |
| `strong` | 700 / 팥색 | 문장 속 핵심 구절 |
| `.quote` | 16px / 600 / 팥색 | 사장님 말 |

### 3-4. 레이아웃

- 컨테이너: `.inner` = `min(1080~1200px, 92vw)`, `.narrow` = `min(720px, 92vw)`
- 섹션 상하 여백: 64~74px (모바일 52~56px)
- 브레이크포인트: **1000px / 900px / 860px / 720px** — 통일 안 돼 있습니다. 정리 대상입니다.

---

## 4. 이미지 규격

### 상품 사진 (`p-*.jpg`) — 이 규격을 지켜주세요

| 항목 | 값 |
|---|---|
| 크기 | **900 × 900** 정사각형 |
| 배경 | **순백(#ffffff)** |
| 피사체 | **1개만**, 가운데, 가로세로 최대 **660px** (= 사방 여백 확보) |
| 표시 | `object-fit: contain` + 카드 배경 흰색 |

> 기존 사진은 `sharp`로 가공했습니다: 배경 밝기 정규화 → 여백 트림 → 660px 안에 맞춤 → 900×900 흰 캔버스 합성.
> 실제 촬영본으로 교체할 때 **같은 파일명으로 덮어쓰면 코드 수정이 필요 없습니다.**

| 파일 | 쓰이는 곳 |
|---|---|
| `p-anpan.jpg` | 거친 단팥빵 |
| `p-salt.jpg` | 소금빵 단팥 |
| `p-walnut.jpg` | 호두 단팥빵 |
| `p-milk.jpg` | 단팥 우유 |
| `p-anko.jpg` | 팥소 한 통 |
| `p-boricha.jpg` | 보리차 |

### 인스타그램 칸 사진 (6칸)

`anko.jpg` `anpan-whole.jpg` `anpan-cut.jpg` `salt-bread.jpg` `boricha.jpg` `patjuk.jpg`
현장감이 필요한 자리라 흰 배경이 아닙니다. 실제 가게 사진으로 교체하는 게 가장 좋습니다.

### ⚠ 라이선스 — 배포 전 반드시 처리

`images/CREDITS.txt`에 전부 적어뒀습니다. 요약:

- 상품 사진 6장 + 현장 사진 3장 → **CC0 / 퍼블릭 도메인.** 표기 의무 없음.
- **`salt-bread.jpg` (CC BY-SA 4.0), `boricha.jpg` (CC BY-SA 2.0 KR)** → **저작자·라이선스 표기 필요.**
  → 실제 가게 사진으로 교체하면 이 의무가 사라집니다. **가장 깔끔한 해결책입니다.**

---

## 5. 애니메이션 4가지

| 이름 | 위치 | 설명 |
|---|---|---|
| **로고 무쇠솥** | 3페이지 전부, `site.css` / index `<style>` | 나무 주걱이 팥을 휘젓고, 소용돌이가 돌고, 방울이 끓고, 김이 오릅니다. 주걱은 자루 끝(잡은 손)을 축으로 ±11° 왕복. |
| **커서 단팥빵** | 3페이지 전부, `site.js` / index 인라인 | 63px 단팥빵이 마우스를 lerp 0.14로 따라옵니다. 이동 방향으로 최대 ±16° 기울어집니다. |
| **히어로 슬라이드** | `index.html` | 3장 자동 전환(6초) + 좌우 화살표 + 도트. |
| **종이 접기** | `fold.js` | 아래 상세 |

### 종이 접기 연출 (`fold.js`)

수첩 팝업(홈)과 소식받기 폼에서 **접수 버튼을 누르면** 실행됩니다.

```
0    ~ 0.45s   수첩에서 뜯김 (왼쪽 톱니 결 clip-path)
0.52 ~ 1.12s   오른쪽 절반이 왼쪽으로 넘어가며 접힘  ← 실제 rotateY 3D 폴드
1.20 ~ 1.80s   아래쪽 절반이 위로 넘어가며 접힘      ← 실제 rotateX 3D 폴드
1.88 ~ 2.68s   네 겹으로 접힌 종이가 날아감
2.72s          감사 인사 표시
```

**구현 방식** — 접히는 순간 종이(`.paper`)를 **4벌 복제**해 무대를 만듭니다.
세로 절반 두 칸(`q-top`/`q-bot`) 각각에 가로 반 접기 무대를 넣고 동시에 돌려서,
첫 접기가 위아래로 이어져 보이고 그 상태로 두 번째 접기가 일어납니다.
`backface-visibility`로 넘어간 면은 종이 뒷면이 보입니다.

**종이답게 보이려고 넣은 장치** (수정할 때 지우지 마세요):
- 종이 결 = `feTurbulence` 노이즈 + 섬유 줄무늬 2겹 오버레이 (`.grain`)
- 접힌 자국이 접은 뒤에도 남음 — 어두운 선 + 옆에 하이라이트 (`.crease-v/-h`)
- 접힘 각도를 **일부러 178.4° / 177.6°** 로 어긋냄 — 손으로 접은 티
- 넘어가는 동안 밝기 변화 (빛 받았다 그늘로)
- 모서리를 미세하게 다르게 둥글림 + `drop-shadow`

**API**
```js
window.tearAndFold(paperEl, onDone)
// paperEl : .paper 요소. .tear 안에 있어야 원본이 자동으로 숨겨집니다.
// onDone  : 2.72초 뒤 호출. 여기서 감사 화면을 띄웁니다.
// prefers-reduced-motion 이면 연출을 건너뛰고 onDone()을 즉시 호출합니다.
```

> 모든 애니메이션에 `prefers-reduced-motion: reduce` 대응이 들어 있습니다. **새로 추가하는 모션에도 넣어주세요.**

---

## 6. ⚠ 먼저 손봐야 할 것 (우선순위 순)

### 🔴 1. `index.html`의 인라인 CSS·JS를 분리 — 가장 시급

`index.html` 1,514줄 중 **CSS 510줄 + JS 175줄이 파일 안에 박혀 있습니다.**
`menu.html`·`subscribe.html`은 `site.css`/`site.js`를 쓰는데 홈만 따로 놉니다.

**결과:** 색 토큰·로고 애니메이션·GNB·푸터·커서 단팥빵 코드가 **두 벌 존재**합니다.
색 하나 바꾸려면 두 군데를 고쳐야 하고, 한 곳만 고치면 페이지끼리 어긋납니다.

**할 일:** 공통 부분을 `site.css`/`site.js`로 옮기고 `index.html`도 그걸 링크하게. 페이지 고유 스타일만 남기세요.
(주의: `site.js`는 GNB·맨위로·커서를 이미 바인딩합니다. index에서 중복 바인딩되지 않게 인라인 쪽을 지워야 합니다.)

### 🔴 2. 폼이 아무 데도 전송되지 않음

- 홈 수첩 팝업 (예약: 성함/연락처/날짜/수량/메모)
- 소식받기 (이름/이메일)

둘 다 `preventDefault()` 후 화면만 바꿉니다. **입력값은 어디에도 저장되지 않습니다.**
백엔드·폼 서비스(Formspree, Google Forms 등)·이메일 연동이 필요합니다.
개인정보를 받게 되므로 **처리방침·수집 동의 절차도 같이** 설계해주세요.

### 🟡 3. 가짜 정보 교체

`SHOP.md`에서 `[지어냄]` 표시가 붙은 항목은 **전부 제가 지어낸 값**입니다. 실제 정보로 바꿔야 합니다.

| 항목 | 현재 값 (가짜) |
|---|---|
| 주소 | 서울 종로구 옥인길 12, 1층 |
| 전화 | 02-736-2800 |
| 인스타그램 | @halmae_gongbang |
| 카카오톡 | @할매공방 |
| 영업시간·휴무·빵 나오는 시간 | 오전 9시~팥 떨어질 때까지 / 일요일 휴무 / 10시·15시 |

또한 **개업 연도를 아직 못 여쭤봤습니다.** 로고 아래 `SINCE GRANDMA`는 임시 문구입니다.

### 🟡 4. 브라우저 실기기 검증 안 됨

제작 과정에서 **실제 브라우저로 렌더링을 확인하지 못했습니다.** 특히 확인이 필요한 것:

- 3D 폴드 연출 — Safari / iOS에서 `preserve-3d` + `overflow:hidden` 조합
- `backdrop-filter` (GNB) — 구형 브라우저
- `clip-path` 톱니 결 — 렌더링 차이
- 모바일 실기기에서 GNB 드롭다운, 슬라이더 스크롤 스냅

### 🟢 5. 접근성

- 수첩 팝업에 **포커스 트랩이 없습니다.** Tab으로 뒤 콘텐츠까지 이동됩니다. (ESC 닫기·배경 클릭 닫기·포커스 복귀는 되어 있음)
- 히어로 슬라이드에 **자동재생 정지 버튼이 없습니다.**
- 이미지 `alt`는 다 붙어 있습니다.

### 🟢 6. 성능

- 이미지가 **JPEG만** 있습니다. WebP/AVIF + `<picture>` 권장.
- `salt-bread.jpg` 474KB — 가장 무겁습니다.
- 폰트가 **외부 CDN 의존**입니다. 오프라인/사내망에서 깨집니다. self-host 권장.
- 브레이크포인트 4종(1000/900/860/720px)이 뒤섞여 있습니다. 정리 필요.

---

## 7. 콘텐츠 규칙 — 글을 고칠 때 꼭 지켜주세요

`SHOP.md`에 정리된 사장님의 원칙입니다. **디자인보다 이게 우선입니다.**

### 쓰지 않는 말
- "안내드립니다" / "판매합니다" / "빨리 오세요"
> 이건 손님을 *고객*으로 대하는 말투이지, *옆집 사람* 대하는 말투가 아니라서요.

### 보내지 않는 소식
- 새 메뉴 홍보 / 할인 안내 / 이벤트 소식 / 클래스 모집
> `subscribe.html`에 **"보내지 않는 것"** 패널로 명시해 뒀습니다. 마케팅 문구를 새로 넣지 마세요.

### 맨 아래 한 줄 — 절대 건드리면 안 되는 규칙

```html
<p class="grandma">이 모든 레시피는 정순남 님의 손에서 시작되었습니다.</p>
```

- **넣는 곳:** 홈페이지 맨 아래, 딱 한 줄. 3페이지 전부.
- **넣지 않는 곳:** 간판 / 로고 / 첫 화면 헤드라인 / 인스타 소개글
- **본문보다 작게(12.2px), 한 톤 연하게(`#c4bcb5`), 링크 없이.**

> 이유: 할머니는 원래 나서는 걸 안 좋아하셨습니다.
> 찾아보려는 사람만 보게 하는 게 목적입니다. **크게 만들지 마세요.**

### 살려야 할 사장님 문장
"팥이 웃는다" · "할머니 냄새 나는 가게" · "맛보라" · "씹는 맛이 있어야 팥빵이지" ·
"세대가 만나는 빵" · "그게 규칙적이지가 않아요. 사람 손이니까요" ·
"서운하게 그냥 보내드리진 않을게요" · "화요일마다 오시는 이유를 여쭤본 적은 없어요. 그냥 압니다" ·
"편하실 때 들르세요"

---

## 8. 참고한 레이아웃

| 사이트 | 무엇을 가져왔나 |
|---|---|
| [도제식빵](https://dojebakery.com/) | **전체 골격.** 중앙 로고 → 카테고리 GNB → 풀폭 슬라이드 → 제목+3열 카드 슬라이더 → 긴 가로 배너 → 3단 박스 → 인스타그램 → 4단 푸터 |
| [춘천 감자밭](https://gamzabatt.com/main) | 브랜드 스토리 블록, 가치 2단 구성, Location/Contact 2단 |

> 두 곳 다 **쇼핑몰**이라 장바구니·할인가·후기 목록이 있습니다.
> 할매공방은 온라인 판매가 없어서 그 부분은 뺐고, 후기 대신 **박여사님 이야기 한 편**을 넣었습니다.
> 실제 후기를 지어내지 않았습니다. 추가하실 거면 진짜 후기를 받아야 합니다.

---

## 9. 질문이 생기면

- **콘텐츠·말투** → `SHOP.md`가 유일한 출처입니다. 거기 없으면 사장님께 여쭤보세요.
- **사진 라이선스** → `images/CREDITS.txt`
- **`SHOP.md` 맨 아래 "아직 사장님께 안 여쭤본 것"** 목록도 확인해주세요. 개업 연도, 다른 단골 이야기, 호두 단팥빵 포장 상자 — 세 가지가 미해결입니다.
